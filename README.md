1. Install pipenv: https://thinkdiff.net/how-to-use-python-pipenv-in-mac-and-windows-1c6dc87b403e
2. From command line, run:

- `pipenv install`
- `pipenv shell`

3. From command line, run:

- `python openai_completion_script.py training.txt`